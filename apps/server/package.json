{
  "name": "@quiz-world/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Express and Socket.io server that owns the authoritative game state",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "socket.io-client": "^4.8.3",
    "tsx": "^4.23.1"
  },
  "dependencies": {
    "@quiz-world/shared": "^0.0.0",
    "express": "^5.2.1",
    "socket.io": "^4.8.3"
  }
}
