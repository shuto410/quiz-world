# AGENTS.md

このリポジトリで作業するエージェント・開発者向けの規約。**作業を始める前に必ず読むこと。**

Quiz World は、出題者（ホスト）と参加者に役割が分かれるリアルタイム早押しクイズ大会プラットフォーム。React SPA + Socket.io サーバー + AWS で構成する。

設計の唯一の正は [`docs/design.md`](./docs/design.md)。本書はそこから「破ると壊れること」だけを抜き出した索引である。判断に迷ったら `docs/design.md` の該当章を読む。**設計と実装が食い違った場合は、先に `docs/design.md` を更新してから実装を直す。**

## 破ってはいけない不変条件

1. **ゲームの正しさはサーバーが持つ。** 早押し順、回答権、スコア、勝敗はすべてサーバーで決定する。クライアントで計算・判定しない。
2. **早押し処理の状態読み書きの間に `await` を入れない。** Node.js が単一スレッドでも `await` は処理を譲るため、非同期化すると順序保証が壊れる。状態遷移は同期の純粋関数に閉じ込め、I/O は呼び出し側で行う。
3. **クライアントから `participantId` を受け取らない。** 操作者は Socket セッションから特定する。ペイロードに参加者IDを含めない。
4. **`RoomState` に派生値を持たない。** 優勝者、順位、`isHost`、`hasBuzzed`、`isCurrentResponder` などクライアントで計算できる値はフィールド化しない。
5. **参加者への配信は必ず変換関数を通す。** `status` が `result` 以外のとき、判定前のテキスト回答（`currentSubmittedAnswer`）は参加者に送らない。生の `RoomState` を参加者に emit しない。
6. **状態配信は 1 箇所に集約する。** ハンドラから直接 `socket.emit` / `io.emit` で状態を送らず、`broadcastRoomState()` を経由する。
7. **ホスト専用操作はサーバー側で権限を検証する。** UI で隠すだけにしない。
8. **共有の型・バリデーションは `packages/shared` に置く。** クライアントとサーバーで定義を二重管理しない。

## 作らないものリスト

MVP では以下を実装しない。「あると便利だから」で追加しない。削除済み機能の復活も含めて禁止。

- アプリ内チャット
- 問題管理機能（問題文・正答・解説をアプリで持たない。ホストは外部資料を読み上げる）
- 回答タイマー、自動進行
- `draft` ステータス（大会は作成直後から参加可能。「大会開始」操作は存在しない）
- 自動正誤判定（判定はホストの手動のみ）
- 公開ルーム一覧
- 観戦者・共同出題者・管理者ロール（`Host` と `Participant` のみ）
- 参加者のログイン / アカウント登録（表示名の入力のみ）
- 外部 VC との API 連携
- マルチインスタンス構成、Redis アダプタ、オートスケーリング、マルチリージョン展開

## 特に踏み抜きやすい罠

- **CDK の `ec2.Vpc` はデフォルトで NAT Gateway を作る。** 必ず `natGateways: 0` を指定する。放置すると月数十ドルの固定費になる。
- **`AppStack` は破棄可能に保つ。** 永続データは `PersistentStack`（DynamoDB / ECR）にのみ置く。
- **カバレッジ目標を満たすためだけのテストを書かない。** 実装をなぞるだけのテストや全モックのテストは、仕様のズレを検出できないので価値がない。
- **招待URLのホスト名をコードに埋め込まない。** 設定値から組み立てる。

## 機構化されているガードレール

規約は可能な範囲で機械的に強制してある。Lint やテストが落ちたら、ルールを緩めるのではなく実装を直す。

| ガードレール                                                                        | 実装場所                                                |
| ----------------------------------------------------------------------------------- | ------------------------------------------------------- |
| `any` と非null断言の禁止                                                            | `eslint.config.mjs`                                     |
| `strict` + `noUncheckedIndexedAccess`                                               | `tsconfig.base.json`                                    |
| ドメイン層での I/O ライブラリ import 禁止                                           | `eslint.config.mjs`（`apps/server/src/domain/**`）      |
| ドメイン層での `await` / `async` / タイマー禁止                                     | 同上                                                    |
| ドメイン層での `Date.now()` / `new Date()` / `Math.random()` 禁止（引数で受け取る） | 同上                                                    |
| 状態配信を `broadcastRoomState()` 経由に限定（生の `room:state` emit を禁止）       | `eslint.config.mjs`（`apps/server/src/**`）             |
| 状態の読み書きの間に `await` を挟めない構造                                         | `RoomRegistry` の `update()` が同期関数しか受け取らない |

## リポジトリ構成

```
apps/web        # Vite + React + TypeScript (SPA)
apps/server     # Express + Socket.io
packages/shared # ドメイン型、Socketイベント型、バリデーション
infra           # AWS CDK (TypeScript)
docs/design.md  # 設計の唯一の正
```

各ワークスペースは実装ステップの進行に合わせて追加する。現時点で存在しないディレクトリがあるのは想定どおり。

`apps/server/src/domain/**` はゲーム状態遷移の純粋関数だけを置く場所で、上記の ESLint ルールが適用される。ここに I/O を持ち込まない。

## コーディング規約

- **全ファイルの冒頭に、英語で仕様コメントを書く。** そのファイルが何を担当し、なぜその形なのかを説明する。
- **コードコメントはすべて英語で書く。** 散文のドキュメント（本書、`docs/`）は日本語でよい。
- **型には英語の jsdoc コメントで用途を書く。** ドメインモデルは `packages/shared/src/types` に集約する。
- **TDD で進める。** 純粋ロジックは先にテストを書く。UI は動作確認を優先し、ロジックを含む部分だけテストする。
- テストは実装ファイルの隣に `*.test.ts` として置く。
- カバレッジは 90% 以上を維持し、100% を目標にする。ドメイン層は 100% を目指す。
- ユーザーとのやり取りは日本語で行う。

## コマンド

| コマンド                | 内容                                                                    |
| ----------------------- | ----------------------------------------------------------------------- |
| `npm run check`         | 型チェック + Lint + フォーマット確認 + テスト。**各ステップの完了条件** |
| `npm run typecheck`     | 全ワークスペースの型チェック                                            |
| `npm run lint`          | ESLint                                                                  |
| `npm run format`        | Prettier で整形                                                         |
| `npm run test`          | Vitest（watch）                                                         |
| `npm run test:run`      | Vitest（1回実行）                                                       |
| `npm run test:coverage` | カバレッジ付きで実行                                                    |

## 進め方

- `docs/design.md` の「実装ステップ」に沿って進める。ステップを飛ばさない。
- **部ごとにブランチを切る**（例: `feat/part1-foundation`）。**1ステップ = 1コミット。**
- 1ステップ終わるごとに作業を止めてレビューを受け、承認後に次へ進む。
- 各ステップの完了時に `npm run check` が通る状態にする。
- 第2部以降は各ステップでブラウザから動作確認できる状態を作る。
