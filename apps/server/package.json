{
  "name": "@quiz-world/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Express and Socket.io server that owns the authoritative game state",
  "scripts": {
    "dev": "node --watch --env-file-if-exists=../../.env.local --import tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "dynalite": "^4.0.0",
    "socket.io-client": "^4.8.3",
    "tsx": "^4.23.1"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1101.0",
    "@aws-sdk/lib-dynamodb": "^3.1101.0",
    "@quiz-world/shared": "^0.0.0",
    "express": "^5.2.1",
    "socket.io": "^4.8.3"
  }
}
